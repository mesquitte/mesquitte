[workspace]
resolver = "2"
members = ["mesquitte-core", "mqtt-codec-kit"]
exclude = ["examples"]

[profile.release]
lto = true
codegen-units = 1
incremental = false

[profile.bench]
lto = true
codegen-units = 1
incremental = false
debug = true
